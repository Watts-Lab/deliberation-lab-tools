import { Stage } from "../../deliberation-empirica/client/src/Stage";

// StageFrame should include a way to select the currently visible stage and display the time of the stage
// Currently just renders the Stage
export function StageFrame() {
    return (
        <Stage />
    );
}