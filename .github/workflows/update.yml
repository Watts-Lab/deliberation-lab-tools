name: Update Extension

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Verify tag commit is on main branch
        run: |
          git fetch origin main
          if ! git merge-base --is-ancestor origin/main "$GITHUB_SHA"; then
            echo "Tag commit is not on main branch. Exiting."
            exit 1
          fi

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "20"

      - name: Install dependencies
        run: npm ci

      - name: Build + Compile Extension
        run: npm run compile-web

      - name: Run VS Code Extension Tests
        run: npm test
        env:
          CI: true

# Commented out workflow for publishing extension, testing
#      - name: Package and publish VS Code extension
#        if: ${{ success() }}
#        env:
#          VSCE_PAT: ${{ secrets.VSCE_PAT }}
#        run: |
#          npm install -g @vscode/vsce
#          vsce publish --pat $VSCE_PAT